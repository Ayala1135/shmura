import Toolbar from '../Toolbar';
import RequestContent from '../RequestContent';

export default function RequestContentScreen() {
    return(
        <>
        <Toolbar/>
        <RequestContent/>
        </>
    )
}